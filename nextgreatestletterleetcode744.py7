class Solution:
    def nextGreatestLetter(self, letters: List[str], target: str) -> str:
        nums=[0]*len(letters)
        target=ord(target)
        for i in range(len(letters)):
            nums[i]=ord(letters[i])
        low=0
        high=len(letters)-1
        while low<=high:
            mid=(low+high)//2
            if nums[mid]>target:
                high=mid-1
            else:
                low=mid+1
        return chr(nums[low % len(nums)])
