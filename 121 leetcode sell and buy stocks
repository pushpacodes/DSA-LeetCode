class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        n=len(prices)
        profit=0
        buy=float('inf')
        for i in range(n):
            if prices[i]<buy:
                buy=prices[i]
                for j in range(i+1,n):
                    if prices[j]>buy:
                        profit=max(prices[j]-buy,profit)
        return profit
